import React from 'react';
import { View, Text } from 'react-native';

const App = () => {
  const body = 'Blank React Native App';

  return (
    <View>
      <Text>{body}</Text>
    </View>
  );
};

export default App;
